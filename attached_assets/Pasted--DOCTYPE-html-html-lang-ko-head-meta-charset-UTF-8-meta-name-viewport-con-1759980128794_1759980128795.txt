<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>개명 후기</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Noto Sans KR', -apple-system, BlinkMacSystemFont, sans-serif;
            background: #0a0a0a;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(255, 107, 237, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(107, 219, 255, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 40% 20%, rgba(167, 107, 255, 0.15) 0%, transparent 50%);
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .container {
            background: rgba(17, 17, 17, 0.7);
            backdrop-filter: blur(30px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 40px;
            padding: 80px 60px;
            max-width: 900px;
            position: relative;
            z-index: 1;
            box-shadow: 
                0 50px 100px rgba(0, 0, 0, 0.5),
                inset 0 1px 1px rgba(255, 255, 255, 0.1);
            animation: fadeIn 1.2s cubic-bezier(0.4, 0, 0.2, 1);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(50px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        h1 {
            font-size: 4.5em;
            text-align: center;
            background: linear-gradient(135deg, #ff6bed 0%, #6bdbff 50%, #a76bff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 70px;
            font-weight: 900;
            letter-spacing: 8px;
            text-transform: uppercase;
            animation: glow 3s ease-in-out infinite;
        }

        @keyframes glow {
            0%, 100% { filter: drop-shadow(0 0 20px rgba(255, 107, 237, 0.5)); }
            50% { filter: drop-shadow(0 0 40px rgba(107, 219, 255, 0.7)); }
        }

        .review-item {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 25px;
            padding: 45px;
            margin-bottom: 35px;
            position: relative;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .review-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 5px;
            background: linear-gradient(180deg, #ff6bed 0%, #6bdbff 50%, #a76bff 100%);
            opacity: 0;
            transition: opacity 0.5s ease;
        }

        .review-item::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(255, 107, 237, 0.1) 0%, rgba(107, 219, 255, 0.1) 100%);
            opacity: 0;
            transition: opacity 0.5s ease;
            border-radius: 25px;
        }

        .review-item:hover {
            transform: translateX(15px) scale(1.02);
            border-color: rgba(255, 107, 237, 0.5);
            box-shadow: 
                0 20px 60px rgba(255, 107, 237, 0.3),
                0 0 100px rgba(107, 219, 255, 0.2);
        }

        .review-item:hover::before {
            opacity: 1;
        }

        .review-item:hover::after {
            opacity: 1;
        }

        .review-item p {
            font-size: 1.35em;
            line-height: 2.1;
            color: #e0e0e0;
            margin: 0;
            position: relative;
            z-index: 1;
            font-weight: 300;
        }

        .highlight {
            background: linear-gradient(135deg, #ff6bed 0%, #6bdbff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
            font-size: 1.1em;
            display: inline-block;
            position: relative;
        }

        .decorative-line {
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(255, 107, 237, 0.5), rgba(107, 219, 255, 0.5), transparent);
            margin: 50px 0;
            position: relative;
        }

        .decorative-line::after {
            content: '✦';
            position: absolute;
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%);
            color: #6bdbff;
            font-size: 1.5em;
            background: rgba(17, 17, 17, 0.9);
            padding: 0 20px;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.5; transform: translate(-50%, -50%) scale(1); }
            50% { opacity: 1; transform: translate(-50%, -50%) scale(1.2); }
        }

        .number {
            position: absolute;
            top: 20px;
            right: 25px;
            font-size: 5em;
            font-weight: 900;
            background: linear-gradient(135deg, rgba(255, 107, 237, 0.1) 0%, rgba(107, 219, 255, 0.1) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1;
            z-index: 0;
        }

        @media (max-width: 768px) {
            .container {
                padding: 50px 35px;
            }

            h1 {
                font-size: 2.8em;
                letter-spacing: 4px;
                margin-bottom: 50px;
            }

            .review-item {
                padding: 35px 30px;
            }

            .review-item p {
                font-size: 1.15em;
                line-height: 1.9;
            }

            .number {
                font-size: 3.5em;
                top: 15px;
                right: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>개명 후기</h1>
        
        <div class="review-item">
            <div class="number">01</div>
            <p>
                <span class="highlight">절 좋아하는 사람이 많아졌어요</span><br>
                예민한 게 사라졌어요<br>
                요즘 돈도 많이 벌어요
            </p>
        </div>

        <div class="decorative-line"></div>

        <div class="review-item">
            <div class="number">02</div>
            <p>
                <span class="highlight">직장과 아파트가 생겼어요</span><br>
                가전제품도 누가 사주셨어요.<br>
                아빠 외도 중이셨는데 정리하고 들어오셨어요<br>
                지금은 소아정신과에서 아이들 진료보고 있는데 마더테레사라고 칭찬받고 인정받아요
            </p>
        </div>
    </div>
</body>
</html>