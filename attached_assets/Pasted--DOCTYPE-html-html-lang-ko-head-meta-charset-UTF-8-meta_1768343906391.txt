<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Naming & Destiny FAQ</title>
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css" />
    
    <style>
        /* [Color Palette & Variables] */
        :root {
            /* [변경] 배경색: 회색 -> 티파니 블루 틴트가 들어간 밝은 배경 */
            --bg-color: #E8F5F4;
            
            --text-primary: #0a2540;
            --text-secondary: #425466;
            --tiffany-blue: #0ABAB5;      
            
            /* [변경] 워터마크: 배경색이 생겼으므로 조금 더 진하게 (12% 투명도) */
            --watermark-color: rgba(10, 186, 181, 0.12); 
        }

        body {
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, Roboto, sans-serif;
            background-color: var(--bg-color);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: var(--text-primary);
            position: relative;
            overflow-x: hidden; /* 가로 스크롤 방지 */
        }

        /* --- [수정됨] 역동적인 코너 워터마크 --- */
        .bg-watermark {
            position: absolute;
            /* 위치 조절: 모서리에 걸치도록 */
            top: -5%;
            right: -5%;
            
            /* [변경] 크기 축소: 60vw -> 30vw (부담스럽지 않은 포인트 크기) */
            font-size: 30vw; 
            
            font-weight: 900;
            color: var(--watermark-color);
            line-height: 1;
            font-family: sans-serif;
            
            /* 역동성 유지: 기울임 */
            transform: rotate(-15deg);
            
            z-index: 0;
            pointer-events: none;
            user-select: none;
        }

        .container {
            width: 100%;
            max-width: 760px;
            padding: 80px 24px;
            position: relative;
            z-index: 1;
        }

        /* --- [헤더 섹션] --- */
        .header-section {
            margin-bottom: 60px;
            text-align: left;
        }

        .sub-title {
            color: var(--tiffany-blue);
            font-size: 14px;
            font-weight: 700;
            letter-spacing: 1.2px;
            text-transform: uppercase;
            margin-bottom: 16px;
            display: inline-block;
        }

        .main-title {
            font-size: 36px;
            font-weight: 800;
            color: var(--text-primary);
            margin: 0 0 20px 0;
            letter-spacing: -0.03em;
            line-height: 1.25;
        }

        .description {
            font-size: 17px;
            color: var(--text-secondary);
            line-height: 1.65;
            margin: 0;
            max-width: 580px;
            word-break: keep-all;
        }

        /* --- [FAQ 리스트] --- */
        /* 선 색상을 배경에 맞춰 조금 더 부드럽게 조정 */
        .faq-list { border-top: 1px solid rgba(10, 186, 181, 0.2); }
        .faq-item { border-bottom: 1px solid rgba(10, 186, 181, 0.2); }

        .faq-question {
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 28px 0;
            background: none;
            border: none;
            cursor: pointer;
            text-align: left;
            transition: all 0.2s ease;
        }

        .question-text {
            font-size: 19px;
            font-weight: 600;
            color: var(--text-primary);
            letter-spacing: -0.02em;
            transition: color 0.2s;
            padding-right: 20px;
        }

        .faq-question:hover .question-text {
            color: var(--tiffany-blue);
        }

        /* 아이콘 */
        .icon-plus {
            width: 24px;
            height: 24px;
            position: relative;
            flex-shrink: 0;
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .icon-plus::before, .icon-plus::after {
            content: '';
            position: absolute;
            background-color: var(--tiffany-blue);
            border-radius: 10px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .icon-plus::before { width: 16px; height: 2px; }
        .icon-plus::after { width: 2px; height: 16px; }

        .faq-item.active .icon-plus {
            transform: rotate(45deg);
        }
        
        .faq-item.active .icon-plus::before, 
        .faq-item.active .icon-plus::after {
            background-color: var(--text-primary); 
        }

        /* 답변 영역 */
        .faq-answer {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.35s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .answer-content {
            font-size: 16px;
            line-height: 1.75;
            color: var(--text-secondary);
            padding-bottom: 32px;
            padding-right: 40px;
        }

        .answer-content strong {
            color: #1a1f36;
            font-weight: 700;
        }

        /* --- [모바일 대응] --- */
        @media (max-width: 600px) {
            /* 모바일 워터마크 크기도 축소 */
            .bg-watermark { 
                font-size: 50vw; /* 90vw -> 50vw */
                top: -2%;
                right: -10%;
            }
            .container { padding: 50px 20px; }
            .main-title { font-size: 28px; }
            .description { font-size: 15px; }
            .question-text { font-size: 17px; }
            .faq-question { padding: 22px 0; }
        }
    </style>
</head>
<body>

    <div class="bg-watermark">FAQ</div>

    <div class="container">
        
        <div class="header-section">
            <span class="sub-title">Naming & Destiny</span>
            <h1 class="main-title">당신의 이름은<br>안녕하십니까?</h1>
            <p class="description">
                이름은 단순히 불리는 소리가 아닙니다. 평생 나의 운명을 좌우하는 나침반입니다.
                잘못된 이름 때문에 고민하고 계신가요? 개명에 대한 진실을 알려드립니다.
            </p>
        </div>

        <div class="faq-list">
            
            <div class="faq-item">
                <button class="faq-question">
                    <span class="question-text">이름이 같으면 삶도 같은가요?</span>
                    <div class="icon-plus"></div>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        같은 한글 이름이라도 <strong>한자가 다르면 운명</strong>이 달라집니다.<br>
                        <strong>한 획</strong> 차이로 운이 확 바뀌고, 운명공동체인 가족에 따라 삶은 더욱 달라집니다.
                    </div>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span class="question-text">이름 안 바꿔도, 노력하면 되지 않을까요?</span>
                    <div class="icon-plus"></div>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        아무리 열심히 달려도, 이름에 들어있는 <strong>흉운이라는 무거운 타이어</strong>를 매단 채로는 한계가 있습니다.<br>
                        타이어를 떼어내면 삶이 훨씬 가벼워집니다.
                    </div>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span class="question-text">개명하면 진짜 좋아지나요?</span>
                    <div class="icon-plus"></div>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        매년 약 <strong>15만 명</strong>이 개명합니다. 상담해 드리면 <strong>개명 전·후 이름대로 살아온 것</strong>에 깜짝 놀랍니다.<br>
                        임상을 해보면, 보통 <strong>개명 후 2년</strong>이면 긍정적 변화가 시작됩니다.
                    </div>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span class="question-text">개명 효과가 사람마다 다르던데요?</span>
                    <div class="icon-plus"></div>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        <strong>95% 이상의 작명소</strong>에는 한글 이름의 9개 운과 한자 이름의 3개 주역운 이론이 없어 흉운이 들어가도 모릅니다.<br>
                        흉운의 증감에 따라 개명 효과가 달라지는 것입니다.
                    </div>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span class="question-text">개명, 어렵지 않나요?</span>
                    <div class="icon-plus"></div>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        법적 개명은 <strong>행복추구권</strong>으로 인정돼 절차와 허가가 쉽습니다. <br>
                        이름 바꾸기가 번거로워 보이지만, <strong>평생 흉운</strong>으로 태클 받으며 사는 것보다 훨씬 쉽습니다.
                    </div>
                </div>
            </div>

        </div>
    </div>

    <script>
        const questions = document.querySelectorAll('.faq-question');

        questions.forEach(question => {
            question.addEventListener('click', () => {
                const item = question.parentNode;
                const answer = item.querySelector('.faq-answer');
                const isActive = item.classList.contains('active');

                if (isActive) {
                    item.classList.remove('active');
                    answer.style.maxHeight = null;
                } else {
                    item.classList.add('active');
                    answer.style.maxHeight = answer.scrollHeight + "px";
                }
            });
        });
    </script>
</body>
</html>