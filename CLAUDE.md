# CLAUDE.md — 공통 작업 규칙

이 파일은 **모든 Claude Code 작업창에 공통으로 적용되는 작업 규칙**이다.

- 프로젝트 구조·페이지·API·기술 스택 설명은 [PROJECT.md](PROJECT.md)를 참조한다. (아키텍처 문서)
- 디자인 기준은 [design_guidelines.md](design_guidelines.md)를 참조한다.
- 이 파일은 **무엇을 만드는가**가 아니라 **어떻게 작업하는가**를 정한다. 위 문서들의 내용을 대체하거나 수정하지 않는다.

---

## 1. 화면·기능 수정 시 Browser 선확인

1. 홈페이지 화면이나 사용자 기능을 수정할 때는, **Claude Desktop Browser에서 수정 전 화면과 동작을 먼저 확인한다.**
2. 필요하면 **클릭, 입력, 페이지 이동을 직접 수행**하고 **DOM, 콘솔, 네트워크 요청, 서버 로그를 확인한다.**
3. **원인이 확인되기 전에는 추측으로 코드를 수정하지 않는다.**
4. 수정 후에는 **테스트와 타입 검사를 실행하고, Browser에서 같은 화면과 동작을 다시 검증한다.**
5. **Browser 검증 없이 화면 수정 작업을 완료했다고 보고하지 않는다.**

### 검증에 쓰는 명령

```bash
npm run check
```

| 목적 | 명령 |
| --- | --- |
| 타입 검사 | `npm run check` |
| KNOP 테스트 | `npm run test:knop` |
| 큐(PG) 테스트 | `npm run test:queue:pg` |
| OTP 테스트 | `npm run test:otp` |
| 인스타그램 테스트 | `npm run test:instagram` |

### 테스트 선택 기준

- **수정 범위와 관련된 테스트 및 타입 검사를 선택해 실행한다.**
- **모든 테스트를 기계적으로 전부 실행하지 않는다.** 관련 없는 테스트를 돌리는 것은 시간 낭비이고, 통과했다는 사실이 이번 수정의 안전을 증명하지도 않는다.
- **다만 공통 구조나 핵심 기능을 변경한 경우에는 전체 회귀 테스트를 고려한다.**

전체 회귀를 고려해야 하는 변경의 예:

| 변경 대상 | 이유 |
| --- | --- |
| `shared/schema.ts`, `shared/jobQueueContract.ts` | 스키마·계약이 전 영역에 공유됨 |
| `server/db.ts`, `server/storage.ts` | 모든 데이터 접근 경로의 공통 기반 |
| `server/routes.ts` | 라우팅·미들웨어 전역 영향 |
| 인증·세션·권한 로직 | 실패 시 전 기능이 영향받음 |
| 빌드·설정 파일 (`vite.config`, `tsconfig`, `package.json`) | 전체 빌드·타입 검사에 영향 |

- 타입 검사(`npm run check`)는 파일 단위가 아니라 프로젝트 전체를 보므로, 코드 수정이 있으면 범위와 무관하게 실행한다.
- 무엇을 실행했고 **무엇을 왜 실행하지 않았는지** 함께 보고한다.
- 결과(통과·실패)는 있는 그대로 보고한다.

---

## 2. 개발 서버 취급

6. **새 개발 서버를 실행하기 전에 같은 프로젝트 서버가 이미 실행 중인지 확인한다.**
7. **기존 서버를 사용할 수 있으면 새 포트로 중복 실행하지 않는다.**
8. **다른 프로젝트나 관련 없는 Node 프로세스는 임의로 종료하지 않는다.**

### 확인 방법

- 서버 실행은 항상 `preview_start`(`.claude/launch.json`의 `kop-dev`)를 사용한다. Bash/PowerShell로 직접 띄우지 않는다.
- 실행 중인 서버 확인: `preview_list`
- 포트 점유 프로세스 확인 후, **커맨드라인에 이 프로젝트 경로가 있는지 확인하기 전에는 종료하지 않는다.**

```powershell
Get-NetTCPConnection -LocalPort 5000 -State Listen | ForEach-Object { (Get-CimInstance Win32_Process -Filter "ProcessId=$($_.OwningProcess)").CommandLine }
```

커맨드라인에 `koreanameacad\KoreaNameAcad`가 포함되어 있고 `server/index.ts`를 실행 중일 때만 이 프로젝트 서버다.

- 서버 인스턴스가 둘 이상 뜨면 **일일 스케줄러(매일 08:40 KST)가 중복 실행되어 운영 DB에 중복 쿼리·중복 작업이 발생한다.** 같은 프로젝트 서버는 한 개만 유지한다.

---

## 3. 승인이 필요한 작업

9. **승인 없이 다음을 하지 않는다.**
   - `git push`
   - 배포(Railway 등 운영 반영)
   - `main` 브랜치 병합
   - 운영 DB 변경 (`npm run db:push`, 마이그레이션 적용, 운영 데이터 수정/삭제)

커밋은 요청받았을 때만 한다. 위 작업이 필요하다고 판단되면 **먼저 무엇을 왜 하려는지 보고하고 승인을 받는다.**

---

## 4. 작업 범위

10. **관련 없는 리팩터링이나 구조 변경은 하지 않는다.**

- 요청받은 범위만 수정한다. 요청 범위를 임의로 줄이거나 넓히지 않는다.
- 작업 중 발견한 별개 문제는 **고치지 말고 보고만 한다.**
- 파일 이동·이름 변경·포맷 일괄 적용 등 diff를 넓히는 변경은 요청이 있을 때만 한다.

---

## 5. 보고 방식

- 소통은 쉬운 일상 언어로 한다. (PROJECT.md의 User Preferences 준수)
- 검증하지 않은 것을 검증했다고 말하지 않는다. 테스트가 실패하면 실패했다고 출력과 함께 보고한다.
- 건너뛴 단계가 있으면 무엇을 왜 건너뛰었는지 명시한다.
